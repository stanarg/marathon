{
  "name": "ba42",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "BA42 — Stan's personal marathon companion (zero-dependency PWA). package.json exists only so Node treats the ES2022 modules as ESM under `node --test`; it ships no dependencies and is inert on GitHub Pages.",
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
